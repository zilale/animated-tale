fastapi==0.115.14
mangum==0.19.0         # ASGI adapter for AWS Lambda
uvicorn==0.29.0        # Optional: only needed for local testing
pydantic==2.11.7
requests==2.32.4
pdfkit==1.0.0          # Only if you're generating PDFs
weasyprint==65.1       # Optional: keep if you're using it for PDF styling